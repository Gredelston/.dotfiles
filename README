Assuming you clone this repo into ~/.dotfiles, you can set up your environment like so:

```
cd ~

echo "" >> .bashrc
echo "# Import my standard .bashrc" >> .bashrc
echo "export DOTFILES=\"$HOME/.dotfiles\"" >> .bashrc
echo "source $DOTFILES/.bashrc" >> .bashrc

ln .tmux.conf .dotfiles/.tmux.conf
```
